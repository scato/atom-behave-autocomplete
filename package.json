{
  "name": "behave-autocomplete",
  "main": "./lib/main",
  "version": "0.0.3",
  "description": "Autocompletes based on your defined behave steps",
  "keywords": [],
  "repository": "https://github.com/scato/atom-behave-autocomplete",
  "license": "MIT",
  "engines": {
    "atom": ">=1.0.0 <2.0.0"
  },
  "providedServices": {
    "autocomplete.provider": {
      "versions": {
        "2.0.0": "getProvider"
      }
    },
    "hyperclick.provider": {
      "versions": {
        "0.0.0": "getHyperclickProvider"
      }
    }
  },
  "devDependencies": {
    "request": "^2.53.0",
    "temp": "^0.8.1"
  },
  "dependencies": {},
  "scripts": {
    "test": "apm test"
  }
}
